# Fixtures

Deterministic journals, schemas, rule programs, and benchmark datasets should live here.

Tracked accepted performance baselines also live here when the pilot launch path needs a
repository-owned fallback that works on a fresh QA machine or CI worker.

Deployment templates also live here when the pilot service needs a repository-owned,
packageable config starting point that can be copied into an operator bundle or CI artifact.
