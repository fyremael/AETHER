# Deployment Fixtures

These files are tracked deployment templates for the hardened pilot service.

- `pilot-service.template.json` is the repository-owned starting point for packaged single-node pilot deployments.

The package builder copies and specializes these templates rather than generating deployment shape from scratch.
